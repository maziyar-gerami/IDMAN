package parsso.idman;
/*
import org.springframework.boot.SpringApplication;
import org.springframework.context.ConfigurableApplicationContext;
import parsso.idman.controllers.UsersControllerTest;
import parsso.idman.repos.UserRepo;

public class Main {
    public static void main(String[] args){
        ConfigurableApplicationContext context = SpringApplication.run(IdmanApplication.class, args);
        new UsersControllerTest(context.getBean(UserRepo.class));
        return;
    }
}
 */
